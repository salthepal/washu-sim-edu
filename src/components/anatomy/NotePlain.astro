---
// On the standard case page the same MDX renders cleanly: the annotation
// metadata is ignored and only the case text (children) is shown.
interface Props {
  kind?: string;
  title?: string;
  note?: string;
}
---
<slot />
